# 公開前レビュー

## 検証環境と再現方法

Windows / Python 3.13.15 / pytest 8.4.2で確認しました。パスはプロジェクトルートからの相対パスです。実データの内容・集計・ハッシュ、ユーザー名、開発PCの配置場所は公開記録に含めません。

```powershell
# README.mdのあるプロジェクトルートで実行
& '.\.venv\Scripts\python.exe' -B -m pytest -q -p no:cacheprovider
```

pytestのキャッシュを無効にし、各テストの入出力は `.test-tmp/<ランダムID>/` に作成します。既存の入力・出力・ログ・仮想環境を更新せずに検証できます。

## 公開サンプルの結果

`samples/public/example.csv` だけを一時フォルダーへコピーし、CLIで検証しました。識別用の架空ラベルと例示用メールドメインを使用し、実在人物・企業の情報を使っていません。

| 項目 | 結果 |
| --- | --- |
| 総件数 | 10 |
| 正常件数 | 3 |
| エラー件数 | 7 |
| 重複件数 | 2（最初の行を含む） |
| CLI終了コード | 1（異常行を検出して処理完了） |
| pytest | **82件PASS、失敗・スキップなし** |

サンプルのテストでは、空白除去、日付正規化、重複ID、必須空欄、不正メール、不正日付、不正数値、複数のエラー理由、入力不変性を確認しています。

## 公開前チェック

| 項目 | 確認内容 |
| --- | --- |
| 非公開ファイルの保護 | data・output・logs・.venvの一覧、内容ハッシュ、更新時刻を前後比較し、一致を確認 |
| 動作の維持 | run.pyとsrc内のソースコードが未変更 |
| 入出力の検証 | 既存の全テストと公開サンプルのCLIテストがPASS |
| ローカル処理 | 実行コードは標準ライブラリと自プロジェクトのモジュールのみ。外部AI/APIへの送信機能なし |
| 公開情報 | 文書・ソース・テスト・サンプル・設定を点検。個人情報、秘密情報、ユーザー名、PC固有の絶対パスを残さない構成 |
| テストのメール例 | 正常形式のメールは例示用ドメイン。不正形式のメールは検証専用文字列 |
| Git除外 | check-ignoreで公開サンプルが除外されず、入力・出力・ログ等が除外されることを確認 |
| 非公開ファイルの追跡 | ls-filesでdata・output・logs・.venvに追跡中のファイルがないことを確認 |
| READMEの手順 | コピーは上書き禁止。既存入力があれば停止し、新しい展開先で試す案内 |
| Git操作 | 既存リポジトリを読み取り確認のみ。初期化・追加・コミット・pushは未実施 |

`.gitignore` の変更はサンプル作成済みに合わせたコメントの更新だけで、除外・許可ルールは維持しています。公開準備ではパッケージの追加導入や外部サービスへの送信を行っていません。

## テスト範囲

正常データ、必須空欄、重複ID、不正メール、不正日付、不正数値、空CSV、必要列不足に加え、複数理由、ID先頭ゼロ、うるう年、数値精度・表記保持、BOM、CP932、追加列、引用符付き改行、列数不一致、予約列、バックアップ、更新失敗時の復元、同時実行、リンク拒否、CLIの終了コードと実行ディレクトリを検証しています。

## 確認の限界

- 実機確認は上記の環境です。すべてのWindows/Pythonの組み合わせの保証ではありません。
- 個人情報・秘密情報の点検は目視とパターン検査です。将来追加される内容の安全性を自動保証しません。
- Git除外は確認時点の状態です。公開直前にも追加対象の内容を確認してください。
- 全行をメモリに保持し、大容量性能の測定・保証は未実施です。
- 2ファイルの完全な同時更新や電源断時の自動復旧は未実装です。
- 数式の無害化・匿名化・暗号化は行いません。ExcelではREADMEの文字列取り込み手順を使用してください。
- バックアップ・ログ・テスト一時領域は自動削除しません。
